# Obtain reference files

The pipeline requires the reference files defined in `config.yaml` -> `sci_params.shared`.

These files can be obtained from our public S3 bucket (see `config.yaml` -> `sci_params.shared` for the URL).

Alternaively you can generate your own versions of the reference files to suit your use case.



